{
  "name": "three-editor",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "kill-port": "node kill-port.js",
    "dev": "node kill-port.js && vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6",
    "vite": "^5.1.0"
  }
}
